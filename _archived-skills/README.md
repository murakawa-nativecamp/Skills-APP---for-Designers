# Archived skills

These skills are kept for reference but are **not active** (they live outside
`plugins/nativecamp-design-skills/skills/`, so the plugin does not load them).

- **figma-to-claude-design-prototype** — Figma画面を Claude Design 上で動くプロトタイプに変換するスキル。現在は使用停止（アーカイブ）。再開する場合は `plugins/nativecamp-design-skills/skills/` に戻し、plugin.json / marketplace.json / README の収録一覧に再掲する。
